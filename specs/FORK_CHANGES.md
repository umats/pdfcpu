# Fork checkpoint

- Upstream base: [`4d0e9ff2e547383e02b6b385ea9e6b48c4cc9f65`](https://github.com/pdfcpu/pdfcpu/commit/4d0e9ff2e547383e02b6b385ea9e6b48c4cc9f65), reporting `v0.16.0-rc.1`. Verified with `git ls-remote https://github.com/pdfcpu/pdfcpu.git HEAD refs/heads/master` (both refs returned this hash). This fork now declares `github.com/umats/pdfcpu`.
- Fork modification at this checkpoint: relaxed Standard V=4/R=4 AESV2 reader compatibility when a referenced crypt filter omits local Length and top-level Length is explicitly 128 bits. Strict mode and negative cases retain rejection. Early Catalog Version parsing is syntax-only so effective version can be used before encryption validation.
- Synthetic regression is generated from the test-only PDF constructor and test-only passwords. The in-memory encrypted PDF is edited at fixed-width positions for the PDF-1.4 header, omitted crypt-filter Length, and Catalog Version override; no xref offsets are rewritten or production data included.
- The module rename updates first-party Go imports (including `internal/`), nested `pkg/samples` and `pkg/testdata` module directives, release linker path, coverage scripts, Docker's local build, and fork API documentation. Historical upstream URLs, copyright, and the Apache-2.0 license remain untouched; no NOTICE file existed in this checkout. Do not integrate into pdfrelay until reviewed independently.
- After the rename: `go mod tidy`, `go test ./...`, `go vet ./...`, `go build ./...`, and Windows amd64/arm64 cross-builds of `cmd/pdfcpu`, `pkg/...`, and `internal/...` passed on Go 1.27.1/darwin/arm64. No tag, PR, merge, or release was made.
