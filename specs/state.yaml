workflow_mode: team-pr
project_cycle:
  current_phase: 4
  completed_phases: [1, 2, 3]
active_flow: fix_bug
handoff:
  last_step_completed: module-rename-and-cross-platform-preflight
  next_skill: verify-work
  open_decisions:
    - UAT held pending Docker access (default socket belongs to another user).
    - Review the module rename and AESV2 exception before integration.
    - Do not publish, tag, merge, or release without explicit approval.
