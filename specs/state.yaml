workflow_mode: team-pr
project_cycle:
  current_phase: 4
  completed_phases: [1, 2, 3]
active_flow: fix_bug
handoff:
  last_step_completed: module-rename-and-cross-platform-preflight
  next_skill: verify-work
  open_decisions:
    - Docker image builds and smokes on Colima; await UAT confirmation.
    - Self-audit passed; independent module review is still required.
    - Do not publish, tag, merge, or release without explicit approval.
